# source/main/utils/bbcode/BBDocument.cpp

> Tree-building callbacks of the BBCode parser.

**Needs** — [`BBDocument.h`](BBDocument.h.md)
**Used by** — callers of [`BBDocument.h`](BBDocument.h.md) (see its Used by)
**Tier floor** — T3

## Purpose

Implements `emit_text`, `open_element`, `close_element` and the parameter element constructor described in [`BBDocument.h`](BBDocument.h.md#tree-building). Split from the header only for compilation reasons.

## State

See header twin.
